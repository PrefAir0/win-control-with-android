[project]
name = "win-controller"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "server",
]

[tool.uv.workspace]
members = [
    "server",
    "client",
]

[tool.uv.sources]
server = { workspace = true }
